{
  "id": "bug-fixes",
  "name": "Bug Fixes (unofficial patch set)",
  "version": "0.18.2",
  "shape": "content",
  "facets": [
    "content",
    "plugin"
  ],
  "modApi": 1,
  "engine": ">=0.10.0",
  "dependencies": {
    "core": "*"
  },
  "author": "neostryder",
  "license": "GPL-2.0-only",
  "description": "Opt-in fixes for bugs in Angband 4.2.6, in the model of an unofficial patch.\n\nEach class of fix is a named toggle, so you can take the set minus any one of them. With this off, core stays faithful to 4.2.6 - bugs included, which is the point of the port.",
  "rules": [
    {
      "flag": "bugfix.stateIntegrity",
      "title": "State integrity fixes",
      "description": "The game's own bookkeeping staying consistent with itself, including across a save and reload. Persists the noise and scent heatmaps so monsters track you identically after a save/reload (#4605); gives the floor object list a deterministic total order so equal-distance items never reorder unexpectedly (#4664); and refuses to commit an object that already carries a created artifact a second time (#4510).",
      "default": true
    },
    {
      "flag": "bugfix.levelGeneration",
      "title": "Level generation fixes",
      "description": "Anything that changes the layout a player walks around in. Vanilla level generation can seal a staircase inside a vault it never tunnelled into. Measured over 15,000 levels: 0.15% stranded, overwhelmingly the up stair, since a level gets only 1-2 up stairs but 3-4 down. This adds one reachable staircase on such a floor. It changes the layout of the levels it repairs, so a character played with this on is not layout-identical to one played without it - kept separate so a player who wants faithful layout is not forced to also give up the text and bookkeeping fixes.",
      "default": true
    }
  ],
  "renamedRuleFlags": {
    "bugfix.noiseScentSave": "bugfix.stateIntegrity",
    "bugfix.objectListOrder": "bugfix.stateIntegrity",
    "bugfix.duplicateArtifact": "bugfix.stateIntegrity",
    "bugfix.stairsReachable": "bugfix.levelGeneration"
  },
  "sections": [
    {
      "id": "bugfix-text-and-history",
      "flag": "bugfix.textAndHistory",
      "title": "Text and history fixes",
      "description": "Item and monster descriptions, and anything else the game writes down or says, that say something the game no longer does or no longer means - no game state changes. Rewrites four gamedata descriptions still written for a two-handed-weapon rule Angband 4.2 dropped (the Two-Handed Great Flail, the Pike, the Trident 'of Wrath' and 'Mundwine' - text only, nothing here changes damage, weight, slot or any other number). Stops a unique re-killed through a shape-change or projection death path from logging a second 'Killed X' history entry (#4245), and applies cosmetic corrections to Angband 4.2.6's own message text. It also corrects the Priest spell name from Light of Manwë to Light of Varda and the blessed property's description from gods to Valar. Measured rather than assumed, and it is a smaller list than you might expect: 4.2.6 puts TWO spaces after a sentence almost everywhere, and exactly FOUR messages break that pattern with one (\"The ground shakes! The ceiling caves in!\"), so this normalizes those four UP to the house style. A sweep for misspellings across every msg()/msgt()/get_check() literal found NONE, so spacing really is all there is to fix today. Purely presentational: no message changes meaning, and nothing about gameplay changes.",
      "default": true,
      "renamedSectionFlags": [
        "bugfix.textAndHistory",
        "bugfix.uniqueKillHistory",
        "bugfix.miscStrings",
        "text-corrections"
      ]
    }
  ],
  "repository": "https://github.com/neostryder/neo-angband-mod-bug-fixes"
}
